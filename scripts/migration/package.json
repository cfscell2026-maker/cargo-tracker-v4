{
  "name": "cargo-migration",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "description": "Import ponctuel Google Sheets (export .xlsx) → PostgreSQL (Supabase). LECTURE SEULE du Sheet : ne modifie jamais la source.",
  "scripts": {
    "migrer": "tsx --env-file=.env importer.ts",
    "verifier": "tsx --env-file=.env importer.ts --verifier-seulement",
    "comptes": "tsx --env-file=.env creer-comptes.ts",
    "historique": "tsx --env-file=.env importer-historique.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
